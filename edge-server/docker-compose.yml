version: '3.8'

services:
  edge-lidar:
    build: .
    container_name: edge-lidar-server
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/backend/data
    environment:
      - PORT=8080
    restart: unless-stopped
